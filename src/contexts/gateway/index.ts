/**
 * Gateway Context
 *
 * Protocol Gateway ACL — mediates access across MCP, HTTP, A2A, and WebSocket protocols.
 * Aggregate root: Gateway
 * Key events: RequestAuthorized, RequestDenied, ProtocolTranslated, RateLimitExceeded
 */
