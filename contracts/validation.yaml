# contracts/validation.yaml
# Generated by /formalize from events/candidates/collaborative-storytelling.yaml
# DO NOT edit manually — re-run /formalize to regenerate

context: "collaborative-storytelling"
formalized: "2026-02-28"

schemas:
  # === Session Context (7 events) ===
  - event: SessionCreated
    version: "v1"
    schema: "contracts/agreed/session-created.v1.schema.yaml"
    role: producer
    enforce: strict

  - event: ParticipantJoined
    version: "v1"
    schema: "contracts/agreed/participant-joined.v1.schema.yaml"
    role: producer
    enforce: strict

  - event: SubmissionReceived
    version: "v1"
    schema: "contracts/agreed/submission-received.v1.schema.yaml"
    role: producer
    enforce: strict

  - event: ContractBundleLoaded
    version: "v1"
    schema: "contracts/agreed/contract-bundle-loaded.v1.schema.yaml"
    role: producer
    enforce: strict

  - event: IntegrationReportLoaded
    version: "v1"
    schema: "contracts/agreed/integration-report-loaded.v1.schema.yaml"
    role: producer
    enforce: strict

  - event: SessionPersisted
    version: "v1"
    schema: "contracts/agreed/session-persisted.v1.schema.yaml"
    role: producer
    enforce: strict

  - event: WorkflowPhaseTransitioned
    version: "v1"
    schema: "contracts/agreed/workflow-phase-transitioned.v1.schema.yaml"
    role: producer
    enforce: warn

  # === JamArtifacts Context (4 events) ===
  - event: JamSessionStarted
    version: "v1"
    schema: "contracts/agreed/jam-session-started.v1.schema.yaml"
    role: producer
    enforce: strict

  - event: ConflictResolved
    version: "v1"
    schema: "contracts/agreed/conflict-resolved.v1.schema.yaml"
    role: producer
    enforce: strict

  - event: AggregateOwnershipAssigned
    version: "v1"
    schema: "contracts/agreed/aggregate-ownership-assigned.v1.schema.yaml"
    role: producer
    enforce: strict

  - event: UnresolvedItemFlagged
    version: "v1"
    schema: "contracts/agreed/unresolved-item-flagged.v1.schema.yaml"
    role: producer
    enforce: strict

  # === AppState Context (5 events) ===
  - event: FileLoaded
    version: "v1"
    schema: "contracts/agreed/file-loaded.v1.schema.yaml"
    role: producer
    enforce: strict

  - event: FileRemoved
    version: "v1"
    schema: "contracts/agreed/file-removed.v1.schema.yaml"
    role: producer
    enforce: warn

  - event: ViewModeChanged
    version: "v1"
    schema: "contracts/agreed/view-mode-changed.v1.schema.yaml"
    role: producer
    enforce: strict

  - event: FilterApplied
    version: "v1"
    schema: "contracts/agreed/filter-applied.v1.schema.yaml"
    role: producer
    enforce: warn

  - event: AggregateSelected
    version: "v1"
    schema: "contracts/agreed/aggregate-selected.v1.schema.yaml"
    role: producer
    enforce: warn

sprint-agent-instructions: |
  Before dispatching sprint agents that produce or consume events:
  1. Check contracts/validation.yaml to identify owned vs consumed events
  2. Owned events: schema at contracts/agreed/ is authoritative — validate against it
  3. Events with enforce: warn have LIKELY confidence — verify with human before shipping
  4. Flag any POSSIBLE-confidence fields in schemas as needing human sign-off before shipping
  5. All 16 events are owned by this solo context — no external consumers to coordinate with

unresolved-flags:
  - "WorkflowPhaseTransitioned: phase transitions are inferred, not explicitly emitted as events"
  - "UnresolvedItemFlagged: no HTTP endpoint exposed — only available via SessionStore API"
  - "FileRemoved: inferred from removeFile method, no explicit event type"
  - "FilterApplied: modeled from toggle methods, no explicit event type"
  - "AggregateSelected: modeled from setter method, no explicit event type"
